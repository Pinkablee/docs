---
title: 'Configuration Guide'
---

The time, in seconds, it takes for the queue to load when first initiated.
```lua cfg.lua
cfg.joinDelay = 60 -- (type: number)
```

Set to true to display the queue status in the server's hostname.
```lua cfg.lua
cfg.displayQueueInHostName = true -- (type: boolean)
```

Display queue information on the server console? (enabled by default)
```lua cfg.lua
cfg.consoleInfo = true -- (type: boolean)
```

Enable Discord authentication on the server console to ensure all data is being retrieved. (enabled by default)
```lua cfg.lua
cfg.debug = true -- (type: boolean)
```

Full points from all roles if true, only highest role's points if false.
```lua cfg.lua
cfg.stackPrio = true -- (type: boolean)
```

Configuration for Discord integration.
```lua cfg.lua
cfg.discord = {
    requireIdentifier = true, -- (type: boolean) Require Discord linking?
    whitelist = false, -- (type: boolean) Enable server whitelist?
    whitelistRoleId = YOUR_WHITELISTED_ROLE_ID_HERE, -- (type: number) Role for whitelist access.
    botToken = 'YOUR_BOT_TOKEN_HERE', -- (type: string) Bot token.
    guildId = 'YOUR_GUILD_ID_HERE', -- (type: string) Discord server ID.
} -- (type: table)
```

Configuration for priority roles.
```lua cfg.lua
cfg.prioRoles = { -- (type: table) prioRoles: Configuration for priority roles.
    {label = 'Founder', roleId = ROLE_ID_HERE, points = 10000}, -- (type: table) Founder role configuration.
    {label = 'Head Developer', roleId = ROLE_ID_HERE, points = 5000}, -- (type: table) Head Developer role configuration.
} -- (type: table)
```

Configuration for adaptive cards with different styles.
```lua cfg.lua
cfg.adaptiveCard = {
    welcome_page = {
        label = 'Connecting to pinkable.dev',
        iconImage = 'https://i.imgur.com/3EaZAP1.png',
        bannerImage = 'https://i.imgur.com/XnHrAJV.png',
        store_button = {label = 'Visit Store', iconImage = 'https://i.imgur.com/CLrYUTP.png', url = 'https://store.pinkable.dev', style = 'default'},
        discord_button = {label = 'Join Discord', iconImage = 'https://i.imgur.com/EKhzTJa.png', url = 'https://discord.gg/pinkable', style = 'default'},
    },
    main_page = {
        store_button = {label = 'Visit Store', iconImage = 'https://i.imgur.com/RWuFBNV.png', url = 'https://pinkable.dev/', style = 'default'},
        discord_button = {label = 'Join Discord', iconImage = 'https://i.imgur.com/ERn1W3t.png', url = 'https://discord.gg/pinkable', style = 'default'}
    }
} -- (type: table)
```

Custom messages used in Queue system.
```lua cfg.lua
cfg.strings = {
    ['welcome'] = 'Welcome, {NAME}!\nLoading account information... ({SECONDS}s)🕑',
    ['loaded'] = 'Welcome, {NAME}! \n\nAccount information successfully loaded ✔️',
    ['reviewing'] = 'Reviewing queue details...',
    ['located_identifiers'] = '{IDENTIFIERS} identifiers located ✔️',
    ['located_tokens'] = '{TOKENS} tokens located ✔️',
    ['waiting'] = 'You are currently in a queue, awaiting your turn to join the server.',
    ['connecting'] = 'Total Points: {POINTS}\n\nCurrent Position: [{POSITION}/{QUEUE_SIZE}]\n\nSession Time: {HOURS}h {MINUTES}m {SECONDS}s 🕑\n\nTired of waiting in the queue? Visit our store and donate to skip the line.',
    ['license_not_found'] = 'Rockstar Social Club account not located. Please ensure you are logged in before attempting to connect.',
    ['discord_not_found'] = 'Your Discord account was not found. Ensure that Discord is open and linked to Cfx.re before connecting.',
    ['duplicate_license'] = 'Another player sharing the same license (License: {LICENSE}) as you has been detected on the server.',
    ['verifying_whitelist'] = 'Verifying whitelist status...',
    ['not_whitelisted'] = 'Access denied. Connection requires whitelisting, and possession of the requisite Discord role is mandatory.',
    ['not_in_discord'] = 'To join, please ensure that you are a member of our Discord server: discord.gg/pinkable.',
    ['error_occurred'] = 'A hiccup in processing your queue information has occurred. Kindly reconnect to address this issue.',
    ['restart'] = 'Currently, joining is not possible as the server is slated for a restart in 1 minute. Kindly await the completion of the restart process before attempting to reconnect.',
} -- (type: table)
```
